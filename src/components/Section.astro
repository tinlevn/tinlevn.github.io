---
interface Props {
  id: string;
  title: string;
}

const { id, title } = Astro.props;
---

<section aria-labelledby={id}>
  <h2 id={id} class="section-heading">{title}</h2>
  <div class="mt-4">
    <slot />
  </div>
</section>
